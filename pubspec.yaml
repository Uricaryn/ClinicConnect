name: crm1
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0-216.1.beta

dependencies:
  animated_background: ^2.0.0
  flutter:
    sdk: flutter
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.13.1
flutter_icons:
   image_path: "icons/logo1.png"
   android: true
   ios: true

flutter:
  uses-material-design: true
  assets:
    - assets/logo.gif

